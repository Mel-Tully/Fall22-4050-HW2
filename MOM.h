#include <stdio.h>
#include <stdlib.h>

void Merge(int ** arr, int l, int mid, int r);
void Partition(int * array, int p);
void MOMSelect(int * array, int k, int l);

void printArray(int * array, int length);

void MergeSort(int * array, int l, int r, int length); 
void Merge(int ** arr, int l, int mid, int r); 
